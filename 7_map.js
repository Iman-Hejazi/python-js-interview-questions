/*
1) using the map function, make a list "square_list" which it's elements are square of "list" items
*/

list = [1,2,3,4]


const square_list = list.map((value)=> value * value)
console.log(square_list)